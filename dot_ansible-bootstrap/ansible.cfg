[defaults]
nocows = True
roles_path = ./roles:/etc/ansible/roles
inventory = inventory
become = true
# Use builtin callback and request YAML-formatted results to avoid loading
# the removed community.general.yaml plugin (use default callback's result_format)
stdout_callback = default
result_format = yaml
